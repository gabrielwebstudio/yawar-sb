import React from 'react'

export default function SectionSmallTitle({ children }) {
    return (
        <p className="text-primary text-sm font-medium uppercase tracking-widest mb-3">{children}</p>

    )
}
